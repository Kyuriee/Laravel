<section class="bg-gradient-to-r from-blue-500 to-purple-600 text-white py-20">
    <div class="container mx-auto px-4 text-center">
        <h1 class="text-4xl md:text-6xl font-bold mb-4">{{ $siteConfig['title'] ?? 'Top Up Games Terbaik' }}</h1>
        <p class="text-xl mb-8">{{ $siteConfig['subtitle'] ?? 'Dapatkan diamond, coin, dan item game favorit Anda dengan harga terjangkau' }}</p>
        <p class="mb-6">{{ $siteConfig['description'] ?? 'Platform top up games modern, support semua game populer.' }}</p>
        <a href="#games" class="bg-white text-blue-600 px-8 py-3 rounded-full font-semibold hover:bg-gray-100 transition">Mulai Top Up</a>
    </div>
</section>